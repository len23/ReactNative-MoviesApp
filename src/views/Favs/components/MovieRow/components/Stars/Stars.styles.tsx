import { StyleSheet } from 'react-native';
import { SizeConstants } from '../../../../../../constants/StyleConstants';

const stylesStars = StyleSheet.create({
  starsContainer: {
    flexDirection: 'row',
    marginTop: SizeConstants.paddingSmall,
  },
});

export default stylesStars;
